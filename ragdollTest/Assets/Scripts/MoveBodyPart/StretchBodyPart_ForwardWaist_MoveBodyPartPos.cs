using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//動かしたい身体のパーツの位置を変更することで身体を伸ばす
public class StretchBodyPart_ForwardWaist_MoveBodyPartPos : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
